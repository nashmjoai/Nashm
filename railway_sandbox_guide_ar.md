# 🚂 دليل تشغيل ميزة تنفيذ الكود (Execute Code) مجاناً على Railway

لتشغيل ميزة الـ Code Sandbox مجاناً بالكامل على Railway دون الحاجة لاشتراك مدفوع، سنقوم بإضافة **خدمة جديدة (Service)** لتشغيل حاوية الـ Sandbox الرسمية من LibreChat داخل مشروعك على لوحة تحكم Railway، ثم ربطها بموقعك.

اتبع هذه الخطوات البسيطة بالتفصيل:

---

## 📌 الخطوة 1: إضافة حاوية الـ Sandbox على Railway

1. اذهب إلى لوحة تحكم مشروعك في **[Railway.app](https://railway.app/)**.
2. اضغط على زر **`+ New`** (أو `Add Service`) في الزاوية العلوية.
3. اختر من القائمة **`Docker Image`** (سوف نقوم بسحب حاوية جاهزة).
4. في مربع البحث، الصق اسم الصورة الرسمية للـ Sandbox واضغط Enter:
   ```text
   ghcr.io/librechat-org/code-api:latest
   ```
5. ستقوم Railway بإنشاء خدمة جديدة وبدء تشغيل حاوية الـ Sandbox فوراً.

---

## 📌 الخطوة 2: ضبط الإعدادات والمتغيرات لحاوية الـ Sandbox

1. اضغط على الصندوق الجديد الخاص بالـ Sandbox (الذي يحمل اسم `code-api`).
2. اذهب إلى تبويب **`Variables`** (المتغيرات).
3. أضف المتغير التالي لتحديد منفذ التشغيل:
   * الاسم: **`PORT`**
   * القيمة: **`5050`**
4. اذهب إلى تبويب **`Settings`** (الإعدادات).
5. انزل لقسم **`Networking`** (الشبكات) واضغط على **`Generate Domain`** (أو انسخ رابط الشبكة الداخلية Private Network والذي ينتهي غالباً بـ `.railway.internal:5050` أو رابط خارجي عام).
   > **💡 نصيحة للسرعة والأمان**: يفضل دائماً استخدام الرابط العام المؤقت الذي تولده Railway (مثال: `https://code-api-production.up.railway.app`) كقيمة في الخطوة التالية.

---

## 📌 الخطوة 3: ربط تطبيق "نشامى" الرئيسي بالـ Sandbox

الآن سنقوم بربط تطبيقك الرئيسي بالـ Sandbox لتفعيل الخدمة:

1. اضغط على صندوق تطبيقك الرئيسي (`nashmai` أو تطبيق الـ API الخاص بنشم).
2. اذهب إلى تبويب **`Variables`** (المتغيرات).
3. أضف متغير بيئة جديد للربط:
   * الاسم: **`LIBRECHAT_CODE_BASEURL`**
   * القيمة: **رابط الـ Sandbox الذي حصلت عليه من الخطوة 2 مع إلحاق `/v1` في نهايته**
     *(مثال: `https://code-api-production.up.railway.app/v1`)*
4. تأكد من تفعيل الميزة في ملف [nashm.yaml](file:///c:/Users/abdel/Desktop/nashmai/nashm.yaml) (عن طريق تعيين `executeCode: true`).
5. ارفع التغييرات إلى المستودع (`git push`) لتقوم Railway بالتحديث التلقائي.

---

🎉 **تهانينا!** بمجرد اكتمال البناء، ستعمل ميزة تنفيذ الأكواد مباشرة، وسيكون البوت قادراً على تشغيل نصوص Python وتوليد ملفات Word و PDF و Excel الفعلية وإعطائك روابط لتحميلها مباشرة مجاناً بالكامل.
