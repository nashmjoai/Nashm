# سياسة الخصوصية والاحتفاظ بالبيانات (Zero-Knowledge Storage & Moderation Policy)

## 1. نموذج التشفير: Encrypted at Rest (Zero-Knowledge Storage)

يعتمد تطبيق **نشم AI** نموذج تشفير التخزين عند السكون (Encrypted at Rest / Zero-Knowledge Storage) لبيانات ومحادثات المستخدمين:

- **أثناء التخزين (Database at Rest)**: تشفر جميع الرسائل وعناوين المحادثات والملفات المرفقة محلياً على جانب العميل (Browser) باستخدام خوارزمية **AES-256-GCM** مع مفاتيح مشتقة عبر **PBKDF2-HMAC-SHA256** (600,000 تكرار).
- **أثناء المعالجة (In-Transit / Processing)**: نظراً لأن التطبيق يعتمد على إرسال الطلبات لنماذج الذكاء الاصطناعي (مثل OpenAI, Gemini)، يتم فك تشفير المحتوى محلياً في متصفح المستخدم ثم إرساله عبر اتصال مشفّر ببروتوكول **HTTPS / TLS** إلى السيرفر لتوجيهه إلى API نموذج الذكاء الاصطناعي وحساب التوكنات والاستهلال.
- **تنبيه شفافية للمستخدم**: السيرفر يرى النص الصريح **لحظياً فقط** وقت المعالجة وتمرير الطلب للـ AI، ثم يُحفظ الرد والنص مشفرين بالكامل في MongoDB ولا يمكن لأي أدمن أو اختراق للداتابيس قراءتها بعد ذلك.

---

## 2. الموديريشن وطلبات البلاغات (Moderation & Audit Policy)

1. **عدم إمكانية المراجعة التراجعية (No Retroactive Content Audit)**:
   - بما أن البيانات مخزنة مشفرة بـ Zero-Knowledge Storage ومفاتيح التشفير موجودة فقط في متصفح المستخدم أو مشتقة من الـ Passphrase الخاصة به، **لا يمتلك فريق إدارة نشم أو الأدمن أي إمكانية تقنية لقراءة أو مراجعة محادثات المستخدم القديمة** في قاعدة البيانات، حتى في حال وجود بلاغ أو طلب قانوني.

2. **السجلات المؤقتة وقت الاستدعاء (Temporary In-Transit Logs)**:
   - قد يتم رصد سجلات تشغيلية مؤقتة (Operational Logs) لأغراض حساب استهلاك التوكنات (Token Consumption) ومنع غسيل الأموال أو الاستغلال المفرط. هذه السجلات تحتوي فقط على (المستخدم، عدد التوكنات، النموذج المستخدم، الطابع الزمني) **بدون الاحتفاظ بنص المحادثة نفسه في سجلات دائمة**.

---

## 3. خطة المراحل القادمة (Roadmap & Deferred Features)

- **[ ] Phase 2: Nextcloud Sync Module**:
  - مزامنة حزمة المفاتيح المشفرة والمحادثات مع سيرفر Nextcloud الخاص بالمستخدم كنسخة احتياطية سحابية مستقلة.
- **[ ] Phase 2: Client-Side Encrypted Search**:
  - البحث في المحادثات المشفرة محلياً داخل المتصفح بدون إرسال فهارس بحث نصية عادية إلى MeiliSearch.
