<div dir="rtl" style="font-family: system-ui, -apple-system, sans-serif; line-height: 1.8; padding: 20px;">

# 🚀 دليل نشر مشروع "نشامى" (Nashm) وقاعدة البيانات على سيرفر VPS بالملي

هذا الدليل يشرح لك بالتفصيل والترتيب من اليمين إلى اليسار كيفية شراء سيرفر خاص (VPS)، ورفع كود مشروعك وقاعدة البيانات عليه، وتشغيله ليكون متاحاً للجميع على شبكة الإنترنت.

---

## 📌 الجزء الأول: شراء السيرفر (VPS) وإعداده
نوصي باستخدام **Hetzner Cloud** لكونها الخيار الأسرع، الأرخص، والأكثر استقراراً في أوروبا والشرق الأوسط.

### 1. خطوات التسجيل والطلب:
1. اذهب إلى موقع **[Hetzner Cloud](https://www.hetzner.com/cloud)** وسجل حساباً جديداً.
2. سيطلب منك الموقع تأكيد هويتك (إجراء روتيني لحماية السيرفرات من الاستخدام السيء).
3. بعد تفعيل الحساب، ادخل على لوحة التحكم (Cloud Console) واضغط على **New Project** وقم بتسميته `Nashm`.
4. اضغط على زر **Add Server** واختر الإعدادات التالية بدقة:
   * **Location (الموقع):** اختر **Falkenstein** (ألمانيا) أو **Helsinki** (فنلندا).
   * **Image (نظام التشغيل):** اختر **Ubuntu 24.04 LTS**.
   * **Type (نوع السيرفر):** اختر باقة **Shared vCPU** ثم اختر سيرفر **CX22** (المواصفات: 2 Cores vCPU, 4 GB RAM, 40 GB NVMe SSD) وتكلفته حوالي **4.70 يورو شهرياً** فقط.
   * **SSH Keys / Password:** اختر **Password** لتصلك كلمة مرور السيرفر على بريدك الإلكتروني مباشرة.
   * اضغط على زر **Create & Buy Now** في الأسفل.
5. خلال 30 ثانية سيعمل السيرفر وستحصل على **عنوان IP** خاص بسيرفرك (مثال: `123.45.67.89`).

---

## 💻 الجزء الثاني: الاتصال بالسيرفر الجديد
سنستخدم نظام التشغيل الخاص بك للاتصال بالسيرفر عبر بروتوكول SSH الآمن.

1. افتح تطبيق **Command Prompt** (موجه الأوامر) أو **PowerShell** في جهاز الكمبيوتر الخاص بك.
2. اكتب الأمر التالي (استبدل `123.45.67.89` بالـ IP الخاص بسيرفرك):
   ```bash
   ssh root@123.45.67.89
   ```
3. عند الاتصال لأول مرة، ستظهر لك رسالة تأكيد، اكتب `yes` واضغط `Enter`.
4. سيطلب منك كلمة المرور (قم بنسخها من بريدك الإلكتروني، ولصقها في الشاشة بالضغط على كليك يمين أو اختصار اللصق ثم اضغط `Enter`).
   > ⚠️ **ملاحظة:** عند كتابة أو لصق كلمة المرور في السيرفر لن تظهر أي نجوم أو أحرف، هذا طبيعي جداً لحماية الخصوصية.
5. سيطلب منك السيرفر تغيير كلمة المرور مباشرة:
   * اكتب كلمة المرور الحالية (التي وصلتك بالإيميل) واضغط `Enter`.
   * اكتب كلمة مرور جديدة قوية من اختيارك واضغط `Enter`.
   * أعد كتابة كلمة المرور الجديدة لتأكيدها واضغط `Enter`.

---

## 🛠️ الجزء الثالث: تهيئة السيرفر وتثبيت البرامج
بمجرد دخولك بنجاح وظهور كلمة `root@...` في شاشة الأوامر، ستقوم بتثبيت بيئة تشغيل المشروع (Docker).

1. قم بتحديث حزم النظام أولاً:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
2. قم بتثبيت **Docker** و **Docker Compose** بأمر واحد رسمي وسريع:
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
   ```

---

## 📂 الجزء الرابع: سحب كود المشروع من GitHub إلى السيرفر
بما أنك رفعت كود مشروعك على GitHub، سنقوم بسحبه مباشرة إلى السيرفر.

### الخيار أ: إذا كان مشروعك عاماً (Public):
نفذ هذا الأمر مباشرة في السيرفر لسحب الكود:
```bash
git clone https://github.com/your-username/your-repo-name.git nashm
cd nashm
```

### الخيار ب: إذا كان مشروعك خاصاً (Private):
تحتاج لإعطاء السيرفر صلاحية الوصول لحساب GitHub الخاص بك عبر مفتاح SSH:
1. أنشئ مفتاح SSH داخل السيرفر:
   ```bash
   ssh-keygen -t ed25519 -C "your-email@example.com"
   ```
   *(اضغط Enter ثلاث مرات متتالية لتجاوز الأسئلة الإضافية).*
2. اعرض المفتاح وانسخه بالكامل:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
3. اذهب إلى حسابك في **GitHub** -> **Settings** -> **SSH and GPG keys** -> اضغط **New SSH Key**، اكتب اسماً للسيرفر في العنوان، ثم الصق الكود الذي نسخته واضغط **Add SSH Key**.
4. الآن، قم بسحب المشروع باستخدام رابط الـ SSH الخاص بالمستودع:
   ```bash
   git clone git@github.com:your-username/your-repo-name.git nashm
   cd nashm
   ```

---

## ⚙️ الجزء الخامس: ضبط الإعدادات وتشغيل المشروع وقاعدة البيانات
قاعدة البيانات ستعمل تلقائياً داخل السيرفر وتكون محمية ومؤمنة بالكامل داخل شبكة Docker الداخلية.

1. قم بإنشاء ملف الإعدادات الخاص بالسيرفر بناءً على ملف المثال:
   ```bash
   cp .env.example .env
   ```
2. افتح الملف للتعديل وإضافة مفاتيح الذكاء الاصطناعي:
   ```bash
   nano .env
   ```
   *   تأكد من إبقاء رابط `MONGO_URI` كما هو: `mongodb://mongodb:27017/Nashm` لتقوم قاعدة البيانات بالعمل محلياً داخل السيرفر مجاناً وبأعلى سرعة استجابة.
   *   قم بإضافة مفاتيح الخدمات التي تحتاجها (مثل `OPENAI_API_KEY`, `GEMINI_API_KEY` وغيرها).
   *   لحفظ الملف: اضغط على `Ctrl + O` then `Enter` في لوحة المفاتيح.
   *   للخروج من المحرر: اضغط على `Ctrl + X`.

3. **تشغيل المشروع بالكامل بضغطة زر واحدة:**
   ```bash
   sudo docker compose -f deploy-compose.yml up -d
   ```

سيتكفل نظام Docker الآن بتحميل قاعدة البيانات MongoDB، وتثبيت المتطلبات، وبناء الواجهة الأمامية والخلفية وتشغيلها كخدمة مستمرة بالخلفية حتى لو قمت بإغلاق حاسوبك الشخصي.

---

## 🌍 الجزء السادس: الوصول للموقع وتحديثه مستقبلاً

### 1. الوصول للموقع:
افتح متصفحك واكتب الـ IP الخاص بالسيرفر مباشرة:
`http://your_server_ip` (مثال: `http://123.45.67.89`)
وستجد موقع "نشامى" يعمل أمامك ومستعد للتسجيل والاستخدام.

### 2. كيفية تحديث الموقع عند تعديل الكود مستقبلاً:
إذا قمت بأي تعديلات برمجية على حاسوبك الشخصي وتريد رفعها للسيرفر:
1. قم برفع التعديلات كالمعتاد إلى مستودعك على GitHub من جهازك.
2. اتصل بالسيرفر عبر SSH واكتب الأوامر التالية:
   ```bash
   cd nashm
   git pull
   sudo docker compose -f deploy-compose.yml up -d --build
   ```
   سيقوم السيرفر بسحب التحديثات وإعادة بناء التطبيق تلقائياً خلال ثوانٍ وبدون أي توقف لقاعدة البيانات.

</div>
