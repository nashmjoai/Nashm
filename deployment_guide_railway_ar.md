<div dir="rtl" style="font-family: system-ui, -apple-system, sans-serif; line-height: 1.8; padding: 20px;">

# 🚂 دليل نشر مشروع "نشم" (Nashm) على منصة Railway بالتفصيل

منصة **Railway.app** هي واحدة من أفضل وأسهل المنصات لنشر التطبيقات السحابية. فهي تتصل مباشرة بـ GitHub الخاص بك، وتبني كود المشروع تلقائياً (Dockerfile)، وتوفر لك قواعد بيانات جاهزة بضغطة زر دون الحاجة لإدارة سيرفرات VPS معقدة.

إليك الخطوات بالتفصيل "بالملي" لرفع وتشغيل موقعك وقاعدة البيانات على Railway:

---

## 📌 الخطوة الأولى: إنشاء حساب على Railway
1. اذهب إلى موقع **[Railway.app](https://railway.app/)**.
2. اضغط على **Login** وسجل دخولك باستخدام حسابك على **GitHub** (هذا مهم جداً لربط مستودعات الكود مباشرة).
3. قم بقبول الشروط وسيفتح لك لوحة التحكم الرئيسية الخاصة بك.

---

## 📌 الخطوة الثانية: إنشاء مشروع جديد وإضافة قاعدة البيانات (MongoDB)
تتميز Railway بأنها تمكنك من تشغيل قاعدة البيانات والتطبيق داخل نفس المشروع ليتم ربطهما تلقائياً.

1. من لوحة التحكم، اضغط على زر **New Project** (أو مشروع جديد).
2. اختر **Provision MongoDB** من القائمة.
3. ستلاحظ أن Railway قامت بإنشاء قاعدة بيانات MongoDB مخصصة لك في ثوانٍ معدودة.
4. اضغط على صندوق الـ **MongoDB** الذي ظهر في الشاشة، ثم اذهب إلى تبويب **Variables** (المتغيرات). ستجد رابط الاتصال جاهزاً باسم `MONGODB_URL` (احفظ هذا الاسم لأننا سنربطه بالتطبيق).

---

## 📌 الخطوة الثالثة: ربط مشروعك من GitHub ورفعه
الآن سنقوم برفع كود تطبيق "نشم" الرئيسي ليقوم بالبناء التلقائي:

1. اضغط على زر **Create** أو زر **New** (الموجود في الزاوية العلوية).
2. اختر **GitHub Repo** (مستودع جيت هاب).
3. اختر مستودع مشروعك `nashmai` من القائمة. 
   *(إذا لم يظهر، اضغط على Configure GitHub App لإعطاء صلاحية الوصول لمستودعك).*
4. اضغط على **Deploy Now**.
5. ستقوم Railway فوراً بقراءة الـ `Dockerfile` الخاص بمشروعك وتبدأ في بناء الموقع تلقائياً.

---

## 📌 الخطوة الرابعة: ضبط متغيرات البيئة (Environment Variables)
التطبيق لن يعمل بشكل صحيح حتى نخبره برابط قاعدة البيانات ومفاتيح الذكاء الاصطناعي.

1. اضغط على صندوق تطبيقك (الذي قمت بربطه من GitHub).
2. اذهب إلى تبويب **Variables**.
3. اضغط على **Add Variable** وأضف المتغيرات التالية:
   * **`MONGO_URI`**: الصق القيمة التالية ليربط تلقائياً بقاعدة بيانات MongoDB التي أنشأناها في الخطوة الثانية:
     ```text
     ${{MongoDB.MONGODB_URL}}
     ```
   * **`PORT`**: اكتب القيمة:
     ```text
     3080
     ```
   * **`NODE_ENV`**: اكتب القيمة:
     ```text
     production
     ```
   * **`MEILI_HOST`** و **`RAG_API_URL`**: إذا لم تكن تستخدم ميزات البحث المتطور والملفات في البداية، يمكنك تركها فارغة أو إضافتها لاحقاً.
   * **مفاتيح الذكاء الاصطناعي:**
     * `OPENAI_API_KEY`: مفتاح OpenAI الخاص بك.
     * `GEMINI_API_KEY`: مفتاح Gemini الخاص بك.
     * `ANTHROPIC_API_KEY`: مفتاح Anthropic الخاص بك.
4. بعد إضافة المتغيرات، ستقوم Railway بإعادة تشغيل التطبيق تلقائياً لتطبيق الإعدادات الجديدة.

---

## 📌 الخطوة الخامسة: الحصول على رابط الموقع العام وتجربته
بشكل افتراضي، لا تقوم Railway بتوليد رابط عام للتطبيق إلا إذا طلبت منها ذلك:

1. اضغط على صندوق تطبيقك مجدداً.
2. اذهب إلى تبويب **Settings** (الإعدادات).
3. انزل لأسفل حتى تصل إلى قسم **Networking** (الشبكات).
4. تحت عنوان **Public Domain**، اضغط على زر **Generate Domain**.
5. ستقوم Railway بتوليد رابط للموقع الخاص بك مباشرة (ينتهي بـ `.up.railway.app`).
6. يمكنك أيضاً ربط دومين خاص بك (Domain) بضغطة زر عبر خيار **Custom Domain**.

---

## 🎉 كيف تقوم بالتحديث مستقبلاً؟
أفضل ميزة في Railway هي التحديث التلقائي:
* في كل مرة تقوم فيها بتعديل الكود على جهازك وترفعه إلى GitHub (`git push`)، ستقوم Railway بسحب الكود الجديد فوراً، وإعادة بناء المشروع وتحديثه بالخلفية دون توقف الموقع ودون الحاجة للدخول وكتابة أي أوامر!

</div>
