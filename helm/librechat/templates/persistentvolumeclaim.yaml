{{- if .Values.Nashm.imageVolume.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "Nashm.fullname" $ }}-images
spec:
  accessModes:
    - {{ .Values.Nashm.imageVolume.accessModes }}
{{- if .Values.Nashm.imageVolume.storageClassName }}
  storageClassName: {{ .Values.Nashm.imageVolume.storageClassName }}
{{- end }}          
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.Nashm.imageVolume.size }}
{{- end }}