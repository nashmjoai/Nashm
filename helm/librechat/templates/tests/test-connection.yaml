apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Nashm.fullname" . }}-test-connection"
  labels:
    {{- include "Nashm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Nashm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
