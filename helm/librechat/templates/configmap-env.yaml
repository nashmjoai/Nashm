kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "Nashm.fullname" $ }}-configenv
data:
  {{- $configEnv := default dict .Values.Nashm.configEnv }}
  {{- $adminPanelUrl := .Values.Nashm.adminPanelUrl }}
  {{- $configAdminPanelUrl := dig "configEnv" "ADMIN_PANEL_URL" "" .Values.Nashm }}
  {{- if (index .Values "Nashm-rag-api" "enabled") }}
  RAG_API_URL: http://{{ include "rag.fullname" (index .Subcharts "Nashm-rag-api") | lower }}.{{ .Release.Namespace | lower }}.svc.cluster.local:8000 
  {{- end }}
  {{- if and (not (dig "configEnv" "MEILI_HOST" "" .Values.Nashm)) .Values.meilisearch.enabled }}
  MEILI_HOST: http://{{ include "meilisearch.fullname" .Subcharts.meilisearch }}.{{ .Release.Namespace | lower }}.svc.cluster.local:7700
  {{- end }}
  {{- if and (not (dig "configEnv" "MONGO_URI" "" .Values.Nashm)) .Values.mongodb.enabled }}
  MONGO_URI: mongodb://{{ include "mongodb.service.nameOverride" .Subcharts.mongodb }}.{{ .Release.Namespace | lower }}.svc.cluster.local:27017/Nashm
  {{- end }}
  {{- if and (not (dig "configEnv" "USE_REDIS" "" .Values.Nashm)) .Values.redis.enabled }}
  USE_REDIS: "true"
  {{- end }}
  {{- if and (not (dig "configEnv" "REDIS_URI" "" .Values.Nashm)) .Values.redis.enabled }}
  REDIS_URI: redis://{{ include "common.names.fullname" .Subcharts.redis }}-master.{{ .Release.Namespace | lower }}.svc.cluster.local:6379
  {{- end }}
  {{- if and $adminPanelUrl (not $configAdminPanelUrl) }}
  ADMIN_PANEL_URL: {{ $adminPanelUrl | quote }}
  {{- end }}
  {{- if $configEnv }}
  {{- $renderedConfigEnv := $configEnv }}
  {{- if and $adminPanelUrl (hasKey $configEnv "ADMIN_PANEL_URL") (not $configAdminPanelUrl) }}
  {{- $renderedConfigEnv = omit $configEnv "ADMIN_PANEL_URL" }}
  {{- end }}
  {{- if $renderedConfigEnv }}
  {{- toYaml $renderedConfigEnv | nindent 2 }}
  {{- end }}
  {{- end }}
